fn main() {
    println!("Use `cargo new` to instantiate a nice clean Rust project");
    println!("Use `cargo build` to compile");
    println!("Use `cargo run` to compile and run the executable");
    println!("Use `cargo check ` to ensure there are no compilation errors without building");
}
